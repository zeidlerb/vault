### ***
### WARNING: DO NOT manually EDIT or MERGE this file, it is generated by 'make ci-config'.
### INSTEAD: Edit or merge the source in config/ then run 'make ci-config'.
### ***
version: 2
jobs:
  bundle-releases:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    steps:
    - checkout
    - restore_cache:
        key: package-cache-v1-031a10c189199b6772daa7f2889500bcbf85950a8a4e75a93436c34488331289-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-47f624dfc47f2ee496fc34336078116ee9c3526907b1a7a051476a173cdef69c-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-96827b3ecebc8b9912e958980ee26b857c2e22e82ee2523721905c6facab0da3-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-e0e780e40fcca807b2209216987b5bd2ae5325fbc206c5d9982becda833d45f0-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-3b9dd3d72e82841a7082517886fe8fe94acbc220754234d46fb01b58923f1b8c-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-70127bb4766b454f476e3ce0bfb0f5d9b50d8bc5e964af218ce4f7bace03ae2a-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-3e673df2d2adae686eac00cd3d0d64e46766a1df81b46a3615b2bdc33c8bbbc4-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-21f4563090ea04393489f63e5bf06501e0904d1a4660c6e8a75b43f98f446581-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-3e92470734c786b0e38130c93327ee82d9d1090585cc5b95970859a41cca8825-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-a39f76f97a1afd030999ece1b03d7376384d4ebf5fbacbe67d6eed86484c3b51-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-ea789be2b99349983da87310940d0b4cfc56b6dc3f3175c026553e1ec0c6aae9-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-04ebbd7a0e25c8eeb393233355d63b00cd46d02b29f3a7b3005571c681a28aef-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-2818655db50bb28ab3e43f6a84f43751c7dc4cd1c8c3252507a27d43b428698c-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-15267e4b880b93fdbc65b026161ff39e2c1ea9362b68ff09078014455f24165c-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-2c177b5b1980f04abddd51cb5fdb6ec5db61df3c04e07f4ee5758ed293dc2c77-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-102a6965e3181da3d0a2484023a55666567acdf54ac15a3a6db44ccc29237825-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: ls -lahR dist/
  install-ui-dependencies:
    docker:
    - image: node:10-buster
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: /src
    steps:
    - checkout
    - restore_cache:
        key: yarn-lock-v1-{{ checksum "ui/yarn.lock" }}
    - run:
        command: |
          cd ui
          yarn install
          npm rebuild node-sass
        name: Install UI dependencies
    - save_cache:
        key: yarn-lock-v1-{{ checksum "ui/yarn.lock" }}
        paths:
        - ui/node_modules
  build_windows_386:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_TAGS: '"vault"- BUNDLE_NAME="vault_0.0.0-dev"- CGOENABLED="0"- EDITION=""- GOARCH="386"- GOOS="windows"- GO_VERSION="1.12.13"- JOB_NAME="build_windows_386"- PACKAGE_NAME="vault_0.0.0-dev_windows_386"- PACKAGE_SPEC_ID="2c177b5b1980f04abddd51cb5fdb6ec5db61df3c04e07f4ee5758ed293dc2c77"- PRODUCT_NAME="vault"- VERSION="0.0.0"- VERSION_FULL="0.0.0-dev"- VERSION_PRE="dev"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile static-load || \
          make -f release.Makefile ui-load || \
          make -f release.Makefile yarn-load || \
          make -f release.Makefile base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-2c177b5b1980f04abddd51cb5fdb6ec5db61df3c04e07f4ee5758ed293dc2c77-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make -f release.Makefile package
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-2c177b5b1980f04abddd51cb5fdb6ec5db61df3c04e07f4ee5758ed293dc2c77-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  test-ui:
    docker:
    - image: node:10-buster
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: /src
    resource_class: medium+
    steps:
    - checkout
    - restore_cache:
        key: yarn-lock-v1-{{ checksum "ui/yarn.lock" }}
    - attach_workspace:
        at: .
    - run:
        command: |
          set -x

          # Install Chrome
          wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub \
            | apt-key add -
          echo "deb http://dl.google.com/linux/chrome/deb/ stable main" \
            | tee /etc/apt/sources.list.d/google-chrome.list
          apt-get update
          apt-get -y install google-chrome-stable
          rm /etc/apt/sources.list.d/google-chrome.list
          rm -rf /var/lib/apt/lists/* /var/cache/apt/*

          # Add ./bin to the PATH so vault binary can be run by Ember tests
          export PATH="${PWD}/bin:${PATH}"

          # Run Ember tests
          cd ui
          mkdir -p test-results/qunit
          yarn test:oss
        name: Test UI
    - store_artifacts:
        path: ui/test-results
    - store_test_results:
        path: ui/test-results
  test-ui-browserstack:
    docker:
    - image: node:10-buster
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: /src
    resource_class: medium+
    steps:
    - checkout
    - restore_cache:
        key: yarn-lock-v1-{{ checksum "ui/yarn.lock" }}
    - attach_workspace:
        at: .
    - run:
        command: |
          # Add ./bin to the PATH so vault binary can be found.
          export PATH="${PWD}"/bin:${PATH}
          make test-ui-browserstack
        name: Run Browserstack Tests
  build_darwin_386:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_TAGS: '"vault"- BUNDLE_NAME="vault_0.0.0-dev"- CGOENABLED="0"- EDITION=""- GOARCH="386"- GOOS="darwin"- GO_VERSION="1.12.13"- JOB_NAME="build_darwin_386"- PACKAGE_NAME="vault_0.0.0-dev_darwin_386"- PACKAGE_SPEC_ID="031a10c189199b6772daa7f2889500bcbf85950a8a4e75a93436c34488331289"- PRODUCT_NAME="vault"- VERSION="0.0.0"- VERSION_FULL="0.0.0-dev"- VERSION_PRE="dev"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile static-load || \
          make -f release.Makefile ui-load || \
          make -f release.Makefile yarn-load || \
          make -f release.Makefile base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-031a10c189199b6772daa7f2889500bcbf85950a8a4e75a93436c34488331289-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make -f release.Makefile package
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-031a10c189199b6772daa7f2889500bcbf85950a8a4e75a93436c34488331289-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  build-go-dev:
    docker:
    - image: golang:1.12.12-buster
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: /src
    steps:
    - checkout
    - restore_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
    - attach_workspace:
        at: .
    - run:
        command: |
          # Move dev UI assets to expected location
          rm -rf ./pkg
          mkdir ./pkg

          # Build dev binary
          make bootstrap dev
        name: Build dev binary
    - persist_to_workspace:
        paths:
        - bin
        root: .
  build_netbsd_386:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_TAGS: '"vault"- BUNDLE_NAME="vault_0.0.0-dev"- CGOENABLED="0"- EDITION=""- GOARCH="386"- GOOS="netbsd"- GO_VERSION="1.12.13"- JOB_NAME="build_netbsd_386"- PACKAGE_NAME="vault_0.0.0-dev_netbsd_386"- PACKAGE_SPEC_ID="a39f76f97a1afd030999ece1b03d7376384d4ebf5fbacbe67d6eed86484c3b51"- PRODUCT_NAME="vault"- VERSION="0.0.0"- VERSION_FULL="0.0.0-dev"- VERSION_PRE="dev"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile static-load || \
          make -f release.Makefile ui-load || \
          make -f release.Makefile yarn-load || \
          make -f release.Makefile base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-a39f76f97a1afd030999ece1b03d7376384d4ebf5fbacbe67d6eed86484c3b51-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make -f release.Makefile package
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-a39f76f97a1afd030999ece1b03d7376384d4ebf5fbacbe67d6eed86484c3b51-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  test-go-race:
    machine: true
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: ~/src
    steps:
    - checkout
    - run:
        command: |
          sudo mkdir /go
          sudo chown -R circleci:circleci /go
        name: Allow circleci user to restore Go modules cache
    - restore_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
    - run:
        command: |
          set -x

          # Install Go
          curl -sSLO "https://dl.google.com/go/go${GO_VERSION}.linux-amd64.tar.gz"
          sudo rm -rf /usr/local/go
          sudo tar -C /usr/local -xzf "go${GO_VERSION}.linux-amd64.tar.gz"
          rm -f "go${GO_VERSION}.linux-amd64.tar.gz"
          export GOPATH=/go
          export PATH="${PATH}:${GOPATH}/bin:/usr/local/go/bin"

          # Install CircleCI CLI
          curl -sSL \
              "https://github.com/CircleCI-Public/circleci-cli/releases/download/v${CIRCLECI_CLI_VERSION}/circleci-cli_${CIRCLECI_CLI_VERSION}_linux_amd64.tar.gz" \
            | sudo tar --overwrite -xz \
              -C /usr/local/bin \
              "circleci-cli_${CIRCLECI_CLI_VERSION}_linux_amd64/circleci"

          # Split Go tests by prior test times
          package_names=$(go list \
              -tags "${GO_TAGS}" \
              ./... \
            | grep -v /integ \
            | grep -v /vendor/ \
            | sort \
            | circleci tests split --split-by=timings --timings-type=classname)

          # Install gotestsum
          curl -sSL "https://github.com/gotestyourself/gotestsum/releases/download/v${GOTESTSUM_VERSION}/gotestsum_${GOTESTSUM_VERSION}_linux_amd64.tar.gz" \
            | sudo tar --overwrite -xz -C /usr/local/bin gotestsum

          # Run tests
          make prep
          mkdir -p test-results/go-test
          CGO_ENABLED= \
            VAULT_ADDR= \
            VAULT_TOKEN= \
            VAULT_DEV_ROOT_TOKEN_ID= \
            VAULT_ACC= \
            gotestsum --format=short-verbose --junitfile test-results/go-test/results.xml -- \
              -tags "${GO_TAGS}" \
              -timeout=60m \
              -parallel=20 \
              -race \
              ${package_names}
        name: Run Go tests
        no_output_timeout: 20m
    - store_artifacts:
        path: test-results
    - store_test_results:
        path: test-results
    environment:
    - CIRCLECI_CLI_VERSION: 0.1.5546
    - GO_TAGS: null
    - GO_VERSION: 1.12.12
    - GOTESTSUM_VERSION: 0.3.3
  build_linux_386:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_TAGS: '"vault"- BUNDLE_NAME="vault_0.0.0-dev"- CGOENABLED="0"- EDITION=""- GOARCH="386"- GOOS="linux"- GO_VERSION="1.12.13"- JOB_NAME="build_linux_386"- PACKAGE_NAME="vault_0.0.0-dev_linux_386"- PACKAGE_SPEC_ID="70127bb4766b454f476e3ce0bfb0f5d9b50d8bc5e964af218ce4f7bace03ae2a"- PRODUCT_NAME="vault"- VERSION="0.0.0"- VERSION_FULL="0.0.0-dev"- VERSION_PRE="dev"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile static-load || \
          make -f release.Makefile ui-load || \
          make -f release.Makefile yarn-load || \
          make -f release.Makefile base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-70127bb4766b454f476e3ce0bfb0f5d9b50d8bc5e964af218ce4f7bace03ae2a-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make -f release.Makefile package
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-70127bb4766b454f476e3ce0bfb0f5d9b50d8bc5e964af218ce4f7bace03ae2a-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  test-go:
    machine: true
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: ~/src
    parallelism: 2
    steps:
    - checkout
    - run:
        command: |
          sudo mkdir /go
          sudo chown -R circleci:circleci /go
        name: Allow circleci user to restore Go modules cache
    - restore_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
    - run:
        command: |
          set -x

          # Install Go
          curl -sSLO "https://dl.google.com/go/go${GO_VERSION}.linux-amd64.tar.gz"
          sudo rm -rf /usr/local/go
          sudo tar -C /usr/local -xzf "go${GO_VERSION}.linux-amd64.tar.gz"
          rm -f "go${GO_VERSION}.linux-amd64.tar.gz"
          export GOPATH=/go
          export PATH="${PATH}:${GOPATH}/bin:/usr/local/go/bin"

          # Install CircleCI CLI
          curl -sSL \
              "https://github.com/CircleCI-Public/circleci-cli/releases/download/v${CIRCLECI_CLI_VERSION}/circleci-cli_${CIRCLECI_CLI_VERSION}_linux_amd64.tar.gz" \
            | sudo tar --overwrite -xz \
              -C /usr/local/bin \
              "circleci-cli_${CIRCLECI_CLI_VERSION}_linux_amd64/circleci"

          # Split Go tests by prior test times
          package_names=$(go list \
              -tags "${GO_TAGS}" \
              ./... \
            | grep -v /integ \
            | grep -v /vendor/ \
            | sort \
            | circleci tests split --split-by=timings --timings-type=classname)

          # Install gotestsum
          curl -sSL "https://github.com/gotestyourself/gotestsum/releases/download/v${GOTESTSUM_VERSION}/gotestsum_${GOTESTSUM_VERSION}_linux_amd64.tar.gz" \
            | sudo tar --overwrite -xz -C /usr/local/bin gotestsum

          # Run tests
          make prep
          mkdir -p test-results/go-test
          CGO_ENABLED= \
            VAULT_ADDR= \
            VAULT_TOKEN= \
            VAULT_DEV_ROOT_TOKEN_ID= \
            VAULT_ACC= \
            gotestsum --format=short-verbose --junitfile test-results/go-test/results.xml -- \
              -tags "${GO_TAGS}" \
              -timeout=60m \
              -parallel=20 \
               \
              ${package_names}
        name: Run Go tests
        no_output_timeout: 20m
    - store_artifacts:
        path: test-results
    - store_test_results:
        path: test-results
    environment:
    - CIRCLECI_CLI_VERSION: 0.1.5546
    - GO_TAGS: null
    - GO_VERSION: 1.12.12
    - GOTESTSUM_VERSION: 0.3.3
  build_linux_arm:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_TAGS: '"vault"- BUNDLE_NAME="vault_0.0.0-dev"- CGOENABLED="0"- EDITION=""- GOARCH="arm"- GOOS="linux"- GO_VERSION="1.12.13"- JOB_NAME="build_linux_arm"- PACKAGE_NAME="vault_0.0.0-dev_linux_arm"- PACKAGE_SPEC_ID="21f4563090ea04393489f63e5bf06501e0904d1a4660c6e8a75b43f98f446581"- PRODUCT_NAME="vault"- VERSION="0.0.0"- VERSION_FULL="0.0.0-dev"- VERSION_PRE="dev"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile static-load || \
          make -f release.Makefile ui-load || \
          make -f release.Makefile yarn-load || \
          make -f release.Makefile base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-21f4563090ea04393489f63e5bf06501e0904d1a4660c6e8a75b43f98f446581-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make -f release.Makefile package
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-21f4563090ea04393489f63e5bf06501e0904d1a4660c6e8a75b43f98f446581-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  build_openbsd_386:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_TAGS: '"vault"- BUNDLE_NAME="vault_0.0.0-dev"- CGOENABLED="0"- EDITION=""- GOARCH="386"- GOOS="openbsd"- GO_VERSION="1.12.13"- JOB_NAME="build_openbsd_386"- PACKAGE_NAME="vault_0.0.0-dev_openbsd_386"- PACKAGE_SPEC_ID="04ebbd7a0e25c8eeb393233355d63b00cd46d02b29f3a7b3005571c681a28aef"- PRODUCT_NAME="vault"- VERSION="0.0.0"- VERSION_FULL="0.0.0-dev"- VERSION_PRE="dev"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile static-load || \
          make -f release.Makefile ui-load || \
          make -f release.Makefile yarn-load || \
          make -f release.Makefile base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-04ebbd7a0e25c8eeb393233355d63b00cd46d02b29f3a7b3005571c681a28aef-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make -f release.Makefile package
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-04ebbd7a0e25c8eeb393233355d63b00cd46d02b29f3a7b3005571c681a28aef-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  build_freebsd_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_TAGS: '"vault"- BUNDLE_NAME="vault_0.0.0-dev"- CGOENABLED="0"- EDITION=""- GOARCH="amd64"- GOOS="freebsd"- GO_VERSION="1.12.13"- JOB_NAME="build_freebsd_amd64"- PACKAGE_NAME="vault_0.0.0-dev_freebsd_amd64"- PACKAGE_SPEC_ID="e0e780e40fcca807b2209216987b5bd2ae5325fbc206c5d9982becda833d45f0"- PRODUCT_NAME="vault"- VERSION="0.0.0"- VERSION_FULL="0.0.0-dev"- VERSION_PRE="dev"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile static-load || \
          make -f release.Makefile ui-load || \
          make -f release.Makefile yarn-load || \
          make -f release.Makefile base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-e0e780e40fcca807b2209216987b5bd2ae5325fbc206c5d9982becda833d45f0-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make -f release.Makefile package
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-e0e780e40fcca807b2209216987b5bd2ae5325fbc206c5d9982becda833d45f0-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  build_solaris_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_TAGS: '"vault"- BUNDLE_NAME="vault_0.0.0-dev"- CGOENABLED="0"- EDITION=""- GOARCH="amd64"- GOOS="solaris"- GO_VERSION="1.12.13"- JOB_NAME="build_solaris_amd64"- PACKAGE_NAME="vault_0.0.0-dev_solaris_amd64"- PACKAGE_SPEC_ID="15267e4b880b93fdbc65b026161ff39e2c1ea9362b68ff09078014455f24165c"- PRODUCT_NAME="vault"- VERSION="0.0.0"- VERSION_FULL="0.0.0-dev"- VERSION_PRE="dev"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile static-load || \
          make -f release.Makefile ui-load || \
          make -f release.Makefile yarn-load || \
          make -f release.Makefile base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-15267e4b880b93fdbc65b026161ff39e2c1ea9362b68ff09078014455f24165c-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make -f release.Makefile package
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-15267e4b880b93fdbc65b026161ff39e2c1ea9362b68ff09078014455f24165c-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  build_windows_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_TAGS: '"vault"- BUNDLE_NAME="vault_0.0.0-dev"- CGOENABLED="0"- EDITION=""- GOARCH="amd64"- GOOS="windows"- GO_VERSION="1.12.13"- JOB_NAME="build_windows_amd64"- PACKAGE_NAME="vault_0.0.0-dev_windows_amd64"- PACKAGE_SPEC_ID="102a6965e3181da3d0a2484023a55666567acdf54ac15a3a6db44ccc29237825"- PRODUCT_NAME="vault"- VERSION="0.0.0"- VERSION_FULL="0.0.0-dev"- VERSION_PRE="dev"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile static-load || \
          make -f release.Makefile ui-load || \
          make -f release.Makefile yarn-load || \
          make -f release.Makefile base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-102a6965e3181da3d0a2484023a55666567acdf54ac15a3a6db44ccc29237825-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make -f release.Makefile package
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-102a6965e3181da3d0a2484023a55666567acdf54ac15a3a6db44ccc29237825-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  build_openbsd_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_TAGS: '"vault"- BUNDLE_NAME="vault_0.0.0-dev"- CGOENABLED="0"- EDITION=""- GOARCH="amd64"- GOOS="openbsd"- GO_VERSION="1.12.13"- JOB_NAME="build_openbsd_amd64"- PACKAGE_NAME="vault_0.0.0-dev_openbsd_amd64"- PACKAGE_SPEC_ID="2818655db50bb28ab3e43f6a84f43751c7dc4cd1c8c3252507a27d43b428698c"- PRODUCT_NAME="vault"- VERSION="0.0.0"- VERSION_FULL="0.0.0-dev"- VERSION_PRE="dev"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile static-load || \
          make -f release.Makefile ui-load || \
          make -f release.Makefile yarn-load || \
          make -f release.Makefile base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-2818655db50bb28ab3e43f6a84f43751c7dc4cd1c8c3252507a27d43b428698c-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make -f release.Makefile package
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-2818655db50bb28ab3e43f6a84f43751c7dc4cd1c8c3252507a27d43b428698c-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  pre-flight-checks:
    docker:
    - image: circleci/buildpack-deps
    environment:
    - CCI_VERSION: 0.1.5691
    shell: /usr/bin/env bash -euo pipefail
    steps:
    - checkout
    - run:
        command: |
          export CCI_PATH=/tmp/circleci-cli/$CCI_VERSION
          mkdir -p $CCI_PATH
          NAME=circleci-cli_${CCI_VERSION}_${ARCH}
          URL=$BASE/v${CCI_VERSION}/${NAME}.tar.gz
          curl -sSL $URL \
            | tar --overwrite --strip-components=1 -xz -C $CCI_PATH "${NAME}/circleci"
          # Add circleci to the path for subsequent steps.
          echo "export PATH=$CCI_PATH:\$PATH" >> $BASH_ENV
          # Done, print some debug info.
          set -x
          . $BASH_ENV
          which circleci
          circleci version
        environment:
          ARCH: linux_amd64
          BASE: https://github.com/CircleCI-Public/circleci-cli/releases/download
        name: Install CircleCI CLI
    - run:
        command: make ci-verify
  install-ui-dependencies:
    docker:
    - environment:
        JOBS: 2
      image: node:10-buster
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: /go/src/github.com/hashicorp/vault
    steps:
    - checkout
    - restore_cache:
        key: yarn-lock-v6-{{ checksum "ui/yarn.lock" }}
    - run:
        command: |
          cd ui
          yarn install
          npm rebuild node-sass
        name: Install UI dependencies
    - save_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
        paths:
        - /go/pkg/mod
  cache-builder-images:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        key: base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - restore_cache:
        key: yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
    - restore_cache:
        key: ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
    - restore_cache:
        key: static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile $NAME-load || make -f release.Makefile $NAME-save
        environment:
          NAME: base
        name: Update base builder image layer
    - run:
        command: |
          make -f release.Makefile $NAME-load || make -f release.Makefile $NAME-save
        environment:
          NAME: yarn
        name: Update yarn builder image layer
    - run:
        command: |
          make -f release.Makefile $NAME-load || make -f release.Makefile $NAME-save
        environment:
          NAME: ui
        name: Update ui builder image layer
    - run:
        command: |
          make -f release.Makefile $NAME-load || make -f release.Makefile $NAME-save
        environment:
          NAME: static
        name: Update static builder image layer
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - save_cache:
        key: base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
        paths:
        - .buildcache/base
    - save_cache:
        key: yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        paths:
        - .buildcache/yarn
    - save_cache:
        key: ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        paths:
        - .buildcache/ui
    - save_cache:
        key: static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - .buildcache/static
  build_freebsd_386:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_TAGS: '"vault"- BUNDLE_NAME="vault_0.0.0-dev"- CGOENABLED="0"- EDITION=""- GOARCH="386"- GOOS="freebsd"- GO_VERSION="1.12.13"- JOB_NAME="build_freebsd_386"- PACKAGE_NAME="vault_0.0.0-dev_freebsd_386"- PACKAGE_SPEC_ID="96827b3ecebc8b9912e958980ee26b857c2e22e82ee2523721905c6facab0da3"- PRODUCT_NAME="vault"- VERSION="0.0.0"- VERSION_FULL="0.0.0-dev"- VERSION_PRE="dev"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile static-load || \
          make -f release.Makefile ui-load || \
          make -f release.Makefile yarn-load || \
          make -f release.Makefile base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-96827b3ecebc8b9912e958980ee26b857c2e22e82ee2523721905c6facab0da3-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make -f release.Makefile package
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-96827b3ecebc8b9912e958980ee26b857c2e22e82ee2523721905c6facab0da3-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  build_linux_arm64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_TAGS: '"vault"- BUNDLE_NAME="vault_0.0.0-dev"- CGOENABLED="0"- EDITION=""- GOARCH="arm64"- GOOS="linux"- GO_VERSION="1.12.13"- JOB_NAME="build_linux_arm64"- PACKAGE_NAME="vault_0.0.0-dev_linux_arm64"- PACKAGE_SPEC_ID="3e92470734c786b0e38130c93327ee82d9d1090585cc5b95970859a41cca8825"- PRODUCT_NAME="vault"- VERSION="0.0.0"- VERSION_FULL="0.0.0-dev"- VERSION_PRE="dev"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile static-load || \
          make -f release.Makefile ui-load || \
          make -f release.Makefile yarn-load || \
          make -f release.Makefile base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-3e92470734c786b0e38130c93327ee82d9d1090585cc5b95970859a41cca8825-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make -f release.Makefile package
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-3e92470734c786b0e38130c93327ee82d9d1090585cc5b95970859a41cca8825-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  build_freebsd_arm:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_TAGS: '"vault"- BUNDLE_NAME="vault_0.0.0-dev"- CGOENABLED="0"- EDITION=""- GOARCH="arm"- GOOS="freebsd"- GO_VERSION="1.12.13"- JOB_NAME="build_freebsd_arm"- PACKAGE_NAME="vault_0.0.0-dev_freebsd_arm"- PACKAGE_SPEC_ID="3b9dd3d72e82841a7082517886fe8fe94acbc220754234d46fb01b58923f1b8c"- PRODUCT_NAME="vault"- VERSION="0.0.0"- VERSION_FULL="0.0.0-dev"- VERSION_PRE="dev"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile static-load || \
          make -f release.Makefile ui-load || \
          make -f release.Makefile yarn-load || \
          make -f release.Makefile base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-3b9dd3d72e82841a7082517886fe8fe94acbc220754234d46fb01b58923f1b8c-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make -f release.Makefile package
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-3b9dd3d72e82841a7082517886fe8fe94acbc220754234d46fb01b58923f1b8c-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  build_linux_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_TAGS: '"vault"- BUNDLE_NAME="vault_0.0.0-dev"- CGOENABLED="0"- EDITION=""- GOARCH="amd64"- GOOS="linux"- GO_VERSION="1.12.13"- JOB_NAME="build_linux_amd64"- PACKAGE_NAME="vault_0.0.0-dev_linux_amd64"- PACKAGE_SPEC_ID="3e673df2d2adae686eac00cd3d0d64e46766a1df81b46a3615b2bdc33c8bbbc4"- PRODUCT_NAME="vault"- VERSION="0.0.0"- VERSION_FULL="0.0.0-dev"- VERSION_PRE="dev"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile static-load || \
          make -f release.Makefile ui-load || \
          make -f release.Makefile yarn-load || \
          make -f release.Makefile base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-3e673df2d2adae686eac00cd3d0d64e46766a1df81b46a3615b2bdc33c8bbbc4-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make -f release.Makefile package
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-3e673df2d2adae686eac00cd3d0d64e46766a1df81b46a3615b2bdc33c8bbbc4-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  build_netbsd_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_TAGS: '"vault"- BUNDLE_NAME="vault_0.0.0-dev"- CGOENABLED="0"- EDITION=""- GOARCH="amd64"- GOOS="netbsd"- GO_VERSION="1.12.13"- JOB_NAME="build_netbsd_amd64"- PACKAGE_NAME="vault_0.0.0-dev_netbsd_amd64"- PACKAGE_SPEC_ID="ea789be2b99349983da87310940d0b4cfc56b6dc3f3175c026553e1ec0c6aae9"- PRODUCT_NAME="vault"- VERSION="0.0.0"- VERSION_FULL="0.0.0-dev"- VERSION_PRE="dev"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile static-load || \
          make -f release.Makefile ui-load || \
          make -f release.Makefile yarn-load || \
          make -f release.Makefile base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-ea789be2b99349983da87310940d0b4cfc56b6dc3f3175c026553e1ec0c6aae9-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make -f release.Makefile package
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-ea789be2b99349983da87310940d0b4cfc56b6dc3f3175c026553e1ec0c6aae9-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  build_darwin_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_TAGS: '"vault"- BUNDLE_NAME="vault_0.0.0-dev"- CGOENABLED="0"- EDITION=""- GOARCH="amd64"- GOOS="darwin"- GO_VERSION="1.12.13"- JOB_NAME="build_darwin_amd64"- PACKAGE_NAME="vault_0.0.0-dev_darwin_amd64"- PACKAGE_SPEC_ID="47f624dfc47f2ee496fc34336078116ee9c3526907b1a7a051476a173cdef69c"- PRODUCT_NAME="vault"- VERSION="0.0.0"- VERSION_FULL="0.0.0-dev"- VERSION_PRE="dev"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: static
        name: Generate static image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: ui
        name: Generate ui image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: yarn
        name: Generate yarn image cache key.
    - run:
        command: |
          mkdir -p .buildcache
          FILE=.buildcache/$NAME-cache-key
          make -f release.Makefile $NAME-id > $FILE
          echo "Cache key for layer '$NAME' is $(cat $FILE); written to $FILE"
        environment:
          NAME: base
        name: Generate base image cache key.
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          make -f release.Makefile static-load || \
          make -f release.Makefile ui-load || \
          make -f release.Makefile yarn-load || \
          make -f release.Makefile base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-47f624dfc47f2ee496fc34336078116ee9c3526907b1a7a051476a173cdef69c-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make -f release.Makefile package
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-47f624dfc47f2ee496fc34336078116ee9c3526907b1a7a051476a173cdef69c-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
workflows:
  build-release:
    jobs:
    - cache-builder-images
    - build_darwin_386:
        requires:
        - cache-builder-images
    - build_darwin_amd64:
        requires:
        - cache-builder-images
    - build_freebsd_386:
        requires:
        - cache-builder-images
    - build_freebsd_amd64:
        requires:
        - cache-builder-images
    - build_freebsd_arm:
        requires:
        - cache-builder-images
    - build_linux_386:
        requires:
        - cache-builder-images
    - build_linux_amd64:
        requires:
        - cache-builder-images
    - build_linux_arm:
        requires:
        - cache-builder-images
    - build_linux_arm64:
        requires:
        - cache-builder-images
    - build_netbsd_386:
        requires:
        - cache-builder-images
    - build_netbsd_amd64:
        requires:
        - cache-builder-images
    - build_openbsd_386:
        requires:
        - cache-builder-images
    - build_openbsd_amd64:
        requires:
        - cache-builder-images
    - build_solaris_amd64:
        requires:
        - cache-builder-images
    - build_windows_386:
        requires:
        - cache-builder-images
    - build_windows_amd64:
        requires:
        - cache-builder-images
    - bundle-releases:
        requires:
        - build_darwin_386
        - build_darwin_amd64
        - build_freebsd_386
        - build_freebsd_amd64
        - build_freebsd_arm
        - build_linux_386
        - build_linux_amd64
        - build_linux_arm
        - build_linux_arm64
        - build_netbsd_386
        - build_netbsd_amd64
        - build_openbsd_386
        - build_openbsd_amd64
        - build_solaris_amd64
        - build_windows_386
        - build_windows_amd64
  ci:
    jobs:
    - pre-flight-checks
    - install-ui-dependencies:
        requires:
        - pre-flight-checks
    - build-go-dev:
        requires:
        - pre-flight-checks
    - test-ui:
        requires:
        - install-ui-dependencies
        - build-go-dev
    - test-ui-browserstack:
        filters:
          branches:
            ignore: /pull\/[0-9]+/
        requires:
        - install-ui-dependencies
        - build-go-dev
    - test-go:
        requires:
        - build-go-dev
    - test-go-race:
        requires:
        - build-go-dev
    - website-docker-image:
        context: vault-docs
  version: 2
