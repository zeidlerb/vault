### ***
### WARNING: DO NOT manually EDIT or MERGE this file, it is generated by 'make ci-config'.
### INSTEAD: Edit or merge the source in config/ then run 'make ci-config'.
### ***
version: 2
jobs:
  package_darwin_386:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: '"package_darwin_386"'
    - BUILD_TAGS: '"vault"'
    - BUNDLE_NAME: '"vault_0.0.0-snapshot"'
    - CGOENABLED: '"0"'
    - GIT_REF: '"HEAD"'
    - GO111MODULE: '"off"'
    - GOARCH: '"386"'
    - GOOS: '"darwin"'
    - GO_VERSION: '"1.12.13"'
    - PACKAGE_NAME: '"vault_0.0.0-snapshot_darwin_386"'
    - PACKAGE_SPEC_ID: '"8ce4174e90910f075a5b68e7c81f9f292faaecf25c94b6d852445c3908aa457a"'
    - PRODUCT_EDITION: '""'
    - PRODUCT_NAME: '"vault"'
    - PRODUCT_VERSION: '"0.0.0-snapshot"'
    - PRODUCT_VERSION_MMP: '"0.0.0"'
    - PRODUCT_VERSION_PRE: '"snapshot"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-8ce4174e90910f075a5b68e7c81f9f292faaecf25c94b6d852445c3908aa457a-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-8ce4174e90910f075a5b68e7c81f9f292faaecf25c94b6d852445c3908aa457a-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  bundle-releases:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    steps:
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        key: package-cache-v1-8ce4174e90910f075a5b68e7c81f9f292faaecf25c94b6d852445c3908aa457a-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-cfa7de5074f6feaa91efbd66642dfdd83666b1f19915d6f67af72ca7854815c4-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-c19c2956ab772eb22b70c988b25b17a7b0333f3032d16e135bc6d88937a8c54b-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-efdb3627f583f7bfe80f1c6ecf090d9d08596ed721f322816daaaa5ab9f6904c-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-33c7131b1b40b813fc517c6a70fcbfd9714f47db190b15c30c7e7c830d466d32-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-a52c953d1d426b8086876185a49afd4299bc7f8fe46bd1d591675b25f710e742-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-4b4c08d7b1bdb3d34b75e3813e372f1db2fea5f951c1900231948057e5b28c4c-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-04cdde53dbec5fd94c36cdec85bcc88d4b080f190ea225e8ecfc24ca7c91d530-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-3559225ef449dbd9cfeebd29ba8d85ad7662d0c88ccc582e5ab21abf1055667e-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-c4320711fe1e67a9bf6d52bc0e38f05e1b1875716f219ee4fb79a1d28bb2416f-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-798cc632b47db457ff40dc92e76efd1b7437594850c4fc9517c43ccf655cff12-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-5ed90e11d88068ef1fc8b4e3943a314837d9def22da88ae293e65b779b58d905-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-7bf667920a24772f396666e4c27f1fe3a202e7a6ba6b53f44ae72879615e1685-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-613c5e9a3e9372f288f5a3552f57d26497fd569b7459b5840c60a3298fc2184c-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-32f1f2f5be32b003c02fdee931cd54f9d5fe9d33b978c44946c76cd1d6134a12-{{ checksum ".buildcache/static-cache-key" }}
    - restore_cache:
        key: package-cache-v1-cd4571987fbe65a7082e9480f4c62e6346cb9a7342a3b135e322b452d96c60fc-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: ls -lahR dist/
  package_linux_arm64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: '"package_linux_arm64"'
    - BUILD_TAGS: '"vault"'
    - BUNDLE_NAME: '"vault_0.0.0-snapshot"'
    - CGOENABLED: '"0"'
    - GIT_REF: '"HEAD"'
    - GO111MODULE: '"off"'
    - GOARCH: '"arm64"'
    - GOOS: '"linux"'
    - GO_VERSION: '"1.12.13"'
    - PACKAGE_NAME: '"vault_0.0.0-snapshot_linux_arm64"'
    - PACKAGE_SPEC_ID: '"3559225ef449dbd9cfeebd29ba8d85ad7662d0c88ccc582e5ab21abf1055667e"'
    - PRODUCT_EDITION: '""'
    - PRODUCT_NAME: '"vault"'
    - PRODUCT_VERSION: '"0.0.0-snapshot"'
    - PRODUCT_VERSION_MMP: '"0.0.0"'
    - PRODUCT_VERSION_PRE: '"snapshot"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-3559225ef449dbd9cfeebd29ba8d85ad7662d0c88ccc582e5ab21abf1055667e-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-3559225ef449dbd9cfeebd29ba8d85ad7662d0c88ccc582e5ab21abf1055667e-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_windows_386:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: '"package_windows_386"'
    - BUILD_TAGS: '"vault"'
    - BUNDLE_NAME: '"vault_0.0.0-snapshot"'
    - CGOENABLED: '"0"'
    - GIT_REF: '"HEAD"'
    - GO111MODULE: '"off"'
    - GOARCH: '"386"'
    - GOOS: '"windows"'
    - GO_VERSION: '"1.12.13"'
    - PACKAGE_NAME: '"vault_0.0.0-snapshot_windows_386"'
    - PACKAGE_SPEC_ID: '"32f1f2f5be32b003c02fdee931cd54f9d5fe9d33b978c44946c76cd1d6134a12"'
    - PRODUCT_EDITION: '""'
    - PRODUCT_NAME: '"vault"'
    - PRODUCT_VERSION: '"0.0.0-snapshot"'
    - PRODUCT_VERSION_MMP: '"0.0.0"'
    - PRODUCT_VERSION_PRE: '"snapshot"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-32f1f2f5be32b003c02fdee931cd54f9d5fe9d33b978c44946c76cd1d6134a12-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-32f1f2f5be32b003c02fdee931cd54f9d5fe9d33b978c44946c76cd1d6134a12-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  install-ui-dependencies:
    docker:
    - image: node:10-buster
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: /src
    steps:
    - checkout
    - restore_cache:
        key: yarn-lock-v1-{{ checksum "ui/yarn.lock" }}
    - run:
        command: |
          cd ui
          yarn install
          npm rebuild node-sass
        name: Install UI dependencies
    - save_cache:
        key: yarn-lock-v1-{{ checksum "ui/yarn.lock" }}
        paths:
        - ui/node_modules
  package_netbsd_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: '"package_netbsd_amd64"'
    - BUILD_TAGS: '"vault"'
    - BUNDLE_NAME: '"vault_0.0.0-snapshot"'
    - CGOENABLED: '"0"'
    - GIT_REF: '"HEAD"'
    - GO111MODULE: '"off"'
    - GOARCH: '"amd64"'
    - GOOS: '"netbsd"'
    - GO_VERSION: '"1.12.13"'
    - PACKAGE_NAME: '"vault_0.0.0-snapshot_netbsd_amd64"'
    - PACKAGE_SPEC_ID: '"798cc632b47db457ff40dc92e76efd1b7437594850c4fc9517c43ccf655cff12"'
    - PRODUCT_EDITION: '""'
    - PRODUCT_NAME: '"vault"'
    - PRODUCT_VERSION: '"0.0.0-snapshot"'
    - PRODUCT_VERSION_MMP: '"0.0.0"'
    - PRODUCT_VERSION_PRE: '"snapshot"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-798cc632b47db457ff40dc92e76efd1b7437594850c4fc9517c43ccf655cff12-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-798cc632b47db457ff40dc92e76efd1b7437594850c4fc9517c43ccf655cff12-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_linux_arm:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: '"package_linux_arm"'
    - BUILD_TAGS: '"vault"'
    - BUNDLE_NAME: '"vault_0.0.0-snapshot"'
    - CGOENABLED: '"0"'
    - GIT_REF: '"HEAD"'
    - GO111MODULE: '"off"'
    - GOARCH: '"arm"'
    - GOOS: '"linux"'
    - GO_VERSION: '"1.12.13"'
    - PACKAGE_NAME: '"vault_0.0.0-snapshot_linux_arm"'
    - PACKAGE_SPEC_ID: '"04cdde53dbec5fd94c36cdec85bcc88d4b080f190ea225e8ecfc24ca7c91d530"'
    - PRODUCT_EDITION: '""'
    - PRODUCT_NAME: '"vault"'
    - PRODUCT_VERSION: '"0.0.0-snapshot"'
    - PRODUCT_VERSION_MMP: '"0.0.0"'
    - PRODUCT_VERSION_PRE: '"snapshot"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-04cdde53dbec5fd94c36cdec85bcc88d4b080f190ea225e8ecfc24ca7c91d530-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-04cdde53dbec5fd94c36cdec85bcc88d4b080f190ea225e8ecfc24ca7c91d530-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_solaris_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: '"package_solaris_amd64"'
    - BUILD_TAGS: '"vault"'
    - BUNDLE_NAME: '"vault_0.0.0-snapshot"'
    - CGOENABLED: '"0"'
    - GIT_REF: '"HEAD"'
    - GO111MODULE: '"off"'
    - GOARCH: '"amd64"'
    - GOOS: '"solaris"'
    - GO_VERSION: '"1.12.13"'
    - PACKAGE_NAME: '"vault_0.0.0-snapshot_solaris_amd64"'
    - PACKAGE_SPEC_ID: '"613c5e9a3e9372f288f5a3552f57d26497fd569b7459b5840c60a3298fc2184c"'
    - PRODUCT_EDITION: '""'
    - PRODUCT_NAME: '"vault"'
    - PRODUCT_VERSION: '"0.0.0-snapshot"'
    - PRODUCT_VERSION_MMP: '"0.0.0"'
    - PRODUCT_VERSION_PRE: '"snapshot"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-613c5e9a3e9372f288f5a3552f57d26497fd569b7459b5840c60a3298fc2184c-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-613c5e9a3e9372f288f5a3552f57d26497fd569b7459b5840c60a3298fc2184c-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  test-ui:
    docker:
    - image: node:10-buster
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: /src
    resource_class: medium+
    steps:
    - checkout
    - restore_cache:
        key: yarn-lock-v1-{{ checksum "ui/yarn.lock" }}
    - attach_workspace:
        at: .
    - run:
        command: |
          set -x

          # Install Chrome
          wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub \
            | apt-key add -
          echo "deb http://dl.google.com/linux/chrome/deb/ stable main" \
            | tee /etc/apt/sources.list.d/google-chrome.list
          apt-get update
          apt-get -y install google-chrome-stable
          rm /etc/apt/sources.list.d/google-chrome.list
          rm -rf /var/lib/apt/lists/* /var/cache/apt/*

          # Add ./bin to the PATH so vault binary can be run by Ember tests
          export PATH="${PWD}/bin:${PATH}"

          # Run Ember tests
          cd ui
          mkdir -p test-results/qunit
          yarn test:oss
        name: Test UI
    - store_artifacts:
        path: ui/test-results
    - store_test_results:
        path: ui/test-results
  test-ui-browserstack:
    docker:
    - image: node:10-buster
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: /src
    resource_class: medium+
    steps:
    - checkout
    - restore_cache:
        key: yarn-lock-v1-{{ checksum "ui/yarn.lock" }}
    - attach_workspace:
        at: .
    - run:
        command: |
          # Add ./bin to the PATH so vault binary can be found.
          export PATH="${PWD}"/bin:${PATH}
          make test-ui-browserstack
        name: Run Browserstack Tests
  package_freebsd_arm:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: '"package_freebsd_arm"'
    - BUILD_TAGS: '"vault"'
    - BUNDLE_NAME: '"vault_0.0.0-snapshot"'
    - CGOENABLED: '"0"'
    - GIT_REF: '"HEAD"'
    - GO111MODULE: '"off"'
    - GOARCH: '"arm"'
    - GOOS: '"freebsd"'
    - GO_VERSION: '"1.12.13"'
    - PACKAGE_NAME: '"vault_0.0.0-snapshot_freebsd_arm"'
    - PACKAGE_SPEC_ID: '"33c7131b1b40b813fc517c6a70fcbfd9714f47db190b15c30c7e7c830d466d32"'
    - PRODUCT_EDITION: '""'
    - PRODUCT_NAME: '"vault"'
    - PRODUCT_VERSION: '"0.0.0-snapshot"'
    - PRODUCT_VERSION_MMP: '"0.0.0"'
    - PRODUCT_VERSION_PRE: '"snapshot"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-33c7131b1b40b813fc517c6a70fcbfd9714f47db190b15c30c7e7c830d466d32-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-33c7131b1b40b813fc517c6a70fcbfd9714f47db190b15c30c7e7c830d466d32-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  build-go-dev:
    docker:
    - image: golang:1.12.12-buster
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: /src
    steps:
    - checkout
    - restore_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
    - attach_workspace:
        at: .
    - run:
        command: |
          # Move dev UI assets to expected location
          rm -rf ./pkg
          mkdir ./pkg

          # Build dev binary
          make bootstrap dev
        name: Build dev binary
    - persist_to_workspace:
        paths:
        - bin
        root: .
  test-go-race:
    machine: true
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: ~/src
    steps:
    - checkout
    - run:
        command: |
          sudo mkdir /go
          sudo chown -R circleci:circleci /go
        name: Allow circleci user to restore Go modules cache
    - restore_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
    - run:
        command: |
          set -x

          # Install Go
          curl -sSLO "https://dl.google.com/go/go${GO_VERSION}.linux-amd64.tar.gz"
          sudo rm -rf /usr/local/go
          sudo tar -C /usr/local -xzf "go${GO_VERSION}.linux-amd64.tar.gz"
          rm -f "go${GO_VERSION}.linux-amd64.tar.gz"
          export GOPATH=/go
          export PATH="${PATH}:${GOPATH}/bin:/usr/local/go/bin"

          # Install CircleCI CLI
          curl -sSL \
              "https://github.com/CircleCI-Public/circleci-cli/releases/download/v${CIRCLECI_CLI_VERSION}/circleci-cli_${CIRCLECI_CLI_VERSION}_linux_amd64.tar.gz" \
            | sudo tar --overwrite -xz \
              -C /usr/local/bin \
              "circleci-cli_${CIRCLECI_CLI_VERSION}_linux_amd64/circleci"

          # Split Go tests by prior test times
          package_names=$(go list \
              -tags "${GO_TAGS}" \
              ./... \
            | grep -v /integ \
            | grep -v /vendor/ \
            | sort \
            | circleci tests split --split-by=timings --timings-type=classname)

          # Install gotestsum
          curl -sSL "https://github.com/gotestyourself/gotestsum/releases/download/v${GOTESTSUM_VERSION}/gotestsum_${GOTESTSUM_VERSION}_linux_amd64.tar.gz" \
            | sudo tar --overwrite -xz -C /usr/local/bin gotestsum

          # Run tests
          make prep
          mkdir -p test-results/go-test
          CGO_ENABLED= \
            VAULT_ADDR= \
            VAULT_TOKEN= \
            VAULT_DEV_ROOT_TOKEN_ID= \
            VAULT_ACC= \
            gotestsum --format=short-verbose --junitfile test-results/go-test/results.xml -- \
              -tags "${GO_TAGS}" \
              -timeout=60m \
              -parallel=20 \
              -race \
              ${package_names}
        name: Run Go tests
        no_output_timeout: 20m
    - store_artifacts:
        path: test-results
    - store_test_results:
        path: test-results
    environment:
    - CIRCLECI_CLI_VERSION: 0.1.5546
    - GO_TAGS: null
    - GO_VERSION: 1.12.12
    - GOTESTSUM_VERSION: 0.3.3
  test-go:
    machine: true
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: ~/src
    parallelism: 2
    steps:
    - checkout
    - run:
        command: |
          sudo mkdir /go
          sudo chown -R circleci:circleci /go
        name: Allow circleci user to restore Go modules cache
    - restore_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
    - run:
        command: |
          set -x

          # Install Go
          curl -sSLO "https://dl.google.com/go/go${GO_VERSION}.linux-amd64.tar.gz"
          sudo rm -rf /usr/local/go
          sudo tar -C /usr/local -xzf "go${GO_VERSION}.linux-amd64.tar.gz"
          rm -f "go${GO_VERSION}.linux-amd64.tar.gz"
          export GOPATH=/go
          export PATH="${PATH}:${GOPATH}/bin:/usr/local/go/bin"

          # Install CircleCI CLI
          curl -sSL \
              "https://github.com/CircleCI-Public/circleci-cli/releases/download/v${CIRCLECI_CLI_VERSION}/circleci-cli_${CIRCLECI_CLI_VERSION}_linux_amd64.tar.gz" \
            | sudo tar --overwrite -xz \
              -C /usr/local/bin \
              "circleci-cli_${CIRCLECI_CLI_VERSION}_linux_amd64/circleci"

          # Split Go tests by prior test times
          package_names=$(go list \
              -tags "${GO_TAGS}" \
              ./... \
            | grep -v /integ \
            | grep -v /vendor/ \
            | sort \
            | circleci tests split --split-by=timings --timings-type=classname)

          # Install gotestsum
          curl -sSL "https://github.com/gotestyourself/gotestsum/releases/download/v${GOTESTSUM_VERSION}/gotestsum_${GOTESTSUM_VERSION}_linux_amd64.tar.gz" \
            | sudo tar --overwrite -xz -C /usr/local/bin gotestsum

          # Run tests
          make prep
          mkdir -p test-results/go-test
          CGO_ENABLED= \
            VAULT_ADDR= \
            VAULT_TOKEN= \
            VAULT_DEV_ROOT_TOKEN_ID= \
            VAULT_ACC= \
            gotestsum --format=short-verbose --junitfile test-results/go-test/results.xml -- \
              -tags "${GO_TAGS}" \
              -timeout=60m \
              -parallel=20 \
               \
              ${package_names}
        name: Run Go tests
        no_output_timeout: 20m
    - store_artifacts:
        path: test-results
    - store_test_results:
        path: test-results
    environment:
    - CIRCLECI_CLI_VERSION: 0.1.5546
    - GO_TAGS: null
    - GO_VERSION: 1.12.12
    - GOTESTSUM_VERSION: 0.3.3
  package_linux_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: '"package_linux_amd64"'
    - BUILD_TAGS: '"vault"'
    - BUNDLE_NAME: '"vault_0.0.0-snapshot"'
    - CGOENABLED: '"0"'
    - GIT_REF: '"HEAD"'
    - GO111MODULE: '"off"'
    - GOARCH: '"amd64"'
    - GOOS: '"linux"'
    - GO_VERSION: '"1.12.13"'
    - PACKAGE_NAME: '"vault_0.0.0-snapshot_linux_amd64"'
    - PACKAGE_SPEC_ID: '"4b4c08d7b1bdb3d34b75e3813e372f1db2fea5f951c1900231948057e5b28c4c"'
    - PRODUCT_EDITION: '""'
    - PRODUCT_NAME: '"vault"'
    - PRODUCT_VERSION: '"0.0.0-snapshot"'
    - PRODUCT_VERSION_MMP: '"0.0.0"'
    - PRODUCT_VERSION_PRE: '"snapshot"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-4b4c08d7b1bdb3d34b75e3813e372f1db2fea5f951c1900231948057e5b28c4c-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-4b4c08d7b1bdb3d34b75e3813e372f1db2fea5f951c1900231948057e5b28c4c-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_windows_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: '"package_windows_amd64"'
    - BUILD_TAGS: '"vault"'
    - BUNDLE_NAME: '"vault_0.0.0-snapshot"'
    - CGOENABLED: '"0"'
    - GIT_REF: '"HEAD"'
    - GO111MODULE: '"off"'
    - GOARCH: '"amd64"'
    - GOOS: '"windows"'
    - GO_VERSION: '"1.12.13"'
    - PACKAGE_NAME: '"vault_0.0.0-snapshot_windows_amd64"'
    - PACKAGE_SPEC_ID: '"cd4571987fbe65a7082e9480f4c62e6346cb9a7342a3b135e322b452d96c60fc"'
    - PRODUCT_EDITION: '""'
    - PRODUCT_NAME: '"vault"'
    - PRODUCT_VERSION: '"0.0.0-snapshot"'
    - PRODUCT_VERSION_MMP: '"0.0.0"'
    - PRODUCT_VERSION_PRE: '"snapshot"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-cd4571987fbe65a7082e9480f4c62e6346cb9a7342a3b135e322b452d96c60fc-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-cd4571987fbe65a7082e9480f4c62e6346cb9a7342a3b135e322b452d96c60fc-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_linux_386:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: '"package_linux_386"'
    - BUILD_TAGS: '"vault"'
    - BUNDLE_NAME: '"vault_0.0.0-snapshot"'
    - CGOENABLED: '"0"'
    - GIT_REF: '"HEAD"'
    - GO111MODULE: '"off"'
    - GOARCH: '"386"'
    - GOOS: '"linux"'
    - GO_VERSION: '"1.12.13"'
    - PACKAGE_NAME: '"vault_0.0.0-snapshot_linux_386"'
    - PACKAGE_SPEC_ID: '"a52c953d1d426b8086876185a49afd4299bc7f8fe46bd1d591675b25f710e742"'
    - PRODUCT_EDITION: '""'
    - PRODUCT_NAME: '"vault"'
    - PRODUCT_VERSION: '"0.0.0-snapshot"'
    - PRODUCT_VERSION_MMP: '"0.0.0"'
    - PRODUCT_VERSION_PRE: '"snapshot"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-a52c953d1d426b8086876185a49afd4299bc7f8fe46bd1d591675b25f710e742-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-a52c953d1d426b8086876185a49afd4299bc7f8fe46bd1d591675b25f710e742-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_freebsd_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: '"package_freebsd_amd64"'
    - BUILD_TAGS: '"vault"'
    - BUNDLE_NAME: '"vault_0.0.0-snapshot"'
    - CGOENABLED: '"0"'
    - GIT_REF: '"HEAD"'
    - GO111MODULE: '"off"'
    - GOARCH: '"amd64"'
    - GOOS: '"freebsd"'
    - GO_VERSION: '"1.12.13"'
    - PACKAGE_NAME: '"vault_0.0.0-snapshot_freebsd_amd64"'
    - PACKAGE_SPEC_ID: '"efdb3627f583f7bfe80f1c6ecf090d9d08596ed721f322816daaaa5ab9f6904c"'
    - PRODUCT_EDITION: '""'
    - PRODUCT_NAME: '"vault"'
    - PRODUCT_VERSION: '"0.0.0-snapshot"'
    - PRODUCT_VERSION_MMP: '"0.0.0"'
    - PRODUCT_VERSION_PRE: '"snapshot"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-efdb3627f583f7bfe80f1c6ecf090d9d08596ed721f322816daaaa5ab9f6904c-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-efdb3627f583f7bfe80f1c6ecf090d9d08596ed721f322816daaaa5ab9f6904c-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_openbsd_386:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: '"package_openbsd_386"'
    - BUILD_TAGS: '"vault"'
    - BUNDLE_NAME: '"vault_0.0.0-snapshot"'
    - CGOENABLED: '"0"'
    - GIT_REF: '"HEAD"'
    - GO111MODULE: '"off"'
    - GOARCH: '"386"'
    - GOOS: '"openbsd"'
    - GO_VERSION: '"1.12.13"'
    - PACKAGE_NAME: '"vault_0.0.0-snapshot_openbsd_386"'
    - PACKAGE_SPEC_ID: '"5ed90e11d88068ef1fc8b4e3943a314837d9def22da88ae293e65b779b58d905"'
    - PRODUCT_EDITION: '""'
    - PRODUCT_NAME: '"vault"'
    - PRODUCT_VERSION: '"0.0.0-snapshot"'
    - PRODUCT_VERSION_MMP: '"0.0.0"'
    - PRODUCT_VERSION_PRE: '"snapshot"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-5ed90e11d88068ef1fc8b4e3943a314837d9def22da88ae293e65b779b58d905-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-5ed90e11d88068ef1fc8b4e3943a314837d9def22da88ae293e65b779b58d905-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_openbsd_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: '"package_openbsd_amd64"'
    - BUILD_TAGS: '"vault"'
    - BUNDLE_NAME: '"vault_0.0.0-snapshot"'
    - CGOENABLED: '"0"'
    - GIT_REF: '"HEAD"'
    - GO111MODULE: '"off"'
    - GOARCH: '"amd64"'
    - GOOS: '"openbsd"'
    - GO_VERSION: '"1.12.13"'
    - PACKAGE_NAME: '"vault_0.0.0-snapshot_openbsd_amd64"'
    - PACKAGE_SPEC_ID: '"7bf667920a24772f396666e4c27f1fe3a202e7a6ba6b53f44ae72879615e1685"'
    - PRODUCT_EDITION: '""'
    - PRODUCT_NAME: '"vault"'
    - PRODUCT_VERSION: '"0.0.0-snapshot"'
    - PRODUCT_VERSION_MMP: '"0.0.0"'
    - PRODUCT_VERSION_PRE: '"snapshot"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-7bf667920a24772f396666e4c27f1fe3a202e7a6ba6b53f44ae72879615e1685-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-7bf667920a24772f396666e4c27f1fe3a202e7a6ba6b53f44ae72879615e1685-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_netbsd_386:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: '"package_netbsd_386"'
    - BUILD_TAGS: '"vault"'
    - BUNDLE_NAME: '"vault_0.0.0-snapshot"'
    - CGOENABLED: '"0"'
    - GIT_REF: '"HEAD"'
    - GO111MODULE: '"off"'
    - GOARCH: '"386"'
    - GOOS: '"netbsd"'
    - GO_VERSION: '"1.12.13"'
    - PACKAGE_NAME: '"vault_0.0.0-snapshot_netbsd_386"'
    - PACKAGE_SPEC_ID: '"c4320711fe1e67a9bf6d52bc0e38f05e1b1875716f219ee4fb79a1d28bb2416f"'
    - PRODUCT_EDITION: '""'
    - PRODUCT_NAME: '"vault"'
    - PRODUCT_VERSION: '"0.0.0-snapshot"'
    - PRODUCT_VERSION_MMP: '"0.0.0"'
    - PRODUCT_VERSION_PRE: '"snapshot"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-c4320711fe1e67a9bf6d52bc0e38f05e1b1875716f219ee4fb79a1d28bb2416f-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-c4320711fe1e67a9bf6d52bc0e38f05e1b1875716f219ee4fb79a1d28bb2416f-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_darwin_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: '"package_darwin_amd64"'
    - BUILD_TAGS: '"vault"'
    - BUNDLE_NAME: '"vault_0.0.0-snapshot"'
    - CGOENABLED: '"0"'
    - GIT_REF: '"HEAD"'
    - GO111MODULE: '"off"'
    - GOARCH: '"amd64"'
    - GOOS: '"darwin"'
    - GO_VERSION: '"1.12.13"'
    - PACKAGE_NAME: '"vault_0.0.0-snapshot_darwin_amd64"'
    - PACKAGE_SPEC_ID: '"cfa7de5074f6feaa91efbd66642dfdd83666b1f19915d6f67af72ca7854815c4"'
    - PRODUCT_EDITION: '""'
    - PRODUCT_NAME: '"vault"'
    - PRODUCT_VERSION: '"0.0.0-snapshot"'
    - PRODUCT_VERSION_MMP: '"0.0.0"'
    - PRODUCT_VERSION_PRE: '"snapshot"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-cfa7de5074f6feaa91efbd66642dfdd83666b1f19915d6f67af72ca7854815c4-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-cfa7de5074f6feaa91efbd66642dfdd83666b1f19915d6f67af72ca7854815c4-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_freebsd_386:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: '"package_freebsd_386"'
    - BUILD_TAGS: '"vault"'
    - BUNDLE_NAME: '"vault_0.0.0-snapshot"'
    - CGOENABLED: '"0"'
    - GIT_REF: '"HEAD"'
    - GO111MODULE: '"off"'
    - GOARCH: '"386"'
    - GOOS: '"freebsd"'
    - GO_VERSION: '"1.12.13"'
    - PACKAGE_NAME: '"vault_0.0.0-snapshot_freebsd_386"'
    - PACKAGE_SPEC_ID: '"c19c2956ab772eb22b70c988b25b17a7b0333f3032d16e135bc6d88937a8c54b"'
    - PRODUCT_EDITION: '""'
    - PRODUCT_NAME: '"vault"'
    - PRODUCT_VERSION: '"0.0.0-snapshot"'
    - PRODUCT_VERSION_MMP: '"0.0.0"'
    - PRODUCT_VERSION_PRE: '"snapshot"'
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v1-c19c2956ab772eb22b70c988b25b17a7b0333f3032d16e135bc6d88937a8c54b-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v1-c19c2956ab772eb22b70c988b25b17a7b0333f3032d16e135bc6d88937a8c54b-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  pre-flight-checks:
    docker:
    - image: circleci/buildpack-deps
    environment:
    - CCI_VERSION: 0.1.5691
    shell: /usr/bin/env bash -euo pipefail
    steps:
    - checkout
    - run:
        command: |
          export CCI_PATH=/tmp/circleci-cli/$CCI_VERSION
          mkdir -p $CCI_PATH
          NAME=circleci-cli_${CCI_VERSION}_${ARCH}
          URL=$BASE/v${CCI_VERSION}/${NAME}.tar.gz
          curl -sSL $URL \
            | tar --overwrite --strip-components=1 -xz -C $CCI_PATH "${NAME}/circleci"
          # Add circleci to the path for subsequent steps.
          echo "export PATH=$CCI_PATH:\$PATH" >> $BASH_ENV
          # Done, print some debug info.
          set -x
          . $BASH_ENV
          which circleci
          circleci version
        environment:
          ARCH: linux_amd64
          BASE: https://github.com/CircleCI-Public/circleci-cli/releases/download
        name: Install CircleCI CLI
    - run:
        command: make ci-verify
  install-ui-dependencies:
    docker:
    - image: node:10-buster
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: /go/src/github.com/hashicorp/vault
    steps:
    - checkout
    - restore_cache:
        key: yarn-lock-v6-{{ checksum "ui/yarn.lock" }}
    - run:
        command: |
          cd ui
          yarn install
          npm rebuild node-sass
        name: Install UI dependencies
    - save_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
        paths:
        - /go/pkg/mod
  cache-builder-images:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        key: base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - restore_cache:
        key: yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
    - restore_cache:
        key: ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
    - restore_cache:
        key: static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK $NAME-load || $MK $NAME-save
        environment:
          MK: make -f release/build/build.mk
          NAME: base
        name: Update base builder image layer
    - run:
        command: |
          $MK $NAME-load || $MK $NAME-save
        environment:
          MK: make -f release/build/build.mk
          NAME: yarn
        name: Update yarn builder image layer
    - run:
        command: |
          $MK $NAME-load || $MK $NAME-save
        environment:
          MK: make -f release/build/build.mk
          NAME: ui
        name: Update ui builder image layer
    - run:
        command: |
          $MK $NAME-load || $MK $NAME-save
        environment:
          MK: make -f release/build/build.mk
          NAME: static
        name: Update static builder image layer
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - save_cache:
        key: base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
        paths:
        - .buildcache/base
    - save_cache:
        key: yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        paths:
        - .buildcache/yarn
    - save_cache:
        key: ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        paths:
        - .buildcache/ui
    - save_cache:
        key: static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - .buildcache/static
workflows:
  build-release:
    jobs:
    - cache-builder-images:
        filters:
          branches:
            only:
            - /build-.*/
            - /ci.*/
    - package_darwin_386:
        requires:
        - cache-builder-images
    - package_darwin_amd64:
        requires:
        - cache-builder-images
    - package_freebsd_386:
        requires:
        - cache-builder-images
    - package_freebsd_amd64:
        requires:
        - cache-builder-images
    - package_freebsd_arm:
        requires:
        - cache-builder-images
    - package_linux_386:
        requires:
        - cache-builder-images
    - package_linux_amd64:
        requires:
        - cache-builder-images
    - package_linux_arm:
        requires:
        - cache-builder-images
    - package_linux_arm64:
        requires:
        - cache-builder-images
    - package_netbsd_386:
        requires:
        - cache-builder-images
    - package_netbsd_amd64:
        requires:
        - cache-builder-images
    - package_openbsd_386:
        requires:
        - cache-builder-images
    - package_openbsd_amd64:
        requires:
        - cache-builder-images
    - package_solaris_amd64:
        requires:
        - cache-builder-images
    - package_windows_386:
        requires:
        - cache-builder-images
    - package_windows_amd64:
        requires:
        - cache-builder-images
    - bundle-releases:
        requires:
        - package_darwin_386
        - package_darwin_amd64
        - package_freebsd_386
        - package_freebsd_amd64
        - package_freebsd_arm
        - package_linux_386
        - package_linux_amd64
        - package_linux_arm
        - package_linux_arm64
        - package_netbsd_386
        - package_netbsd_amd64
        - package_openbsd_386
        - package_openbsd_amd64
        - package_solaris_amd64
        - package_windows_386
        - package_windows_amd64
  ci:
    jobs:
    - pre-flight-checks
    - install-ui-dependencies:
        requires:
        - pre-flight-checks
    - build-go-dev:
        requires:
        - pre-flight-checks
    - test-ui:
        requires:
        - install-ui-dependencies
        - build-go-dev
    - test-ui-browserstack:
        filters:
          branches:
            ignore: /pull\/[0-9]+/
        requires:
        - install-ui-dependencies
        - build-go-dev
    - test-go:
        requires:
        - build-go-dev
    - test-go-race:
        requires:
        - build-go-dev
  version: 2
