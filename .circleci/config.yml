    - PRODUCT_EDITION: ''
    - PRODUCT_NAME: vault
    - PRODUCT_VERSION: 0.0.1-sam-test
    - PRODUCT_VERSION_MMP: 0.0.1
    - PRODUCT_VERSION_PRE: sam-test
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v2-b4a96c53a7d3929a4faee6a7df724e9b837a894c9975b731a90524b78abc64cf-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - run:
        command: ls -lahR dist/
        name: Dump contents of dist/
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v2-b4a96c53a7d3929a4faee6a7df724e9b837a894c9975b731a90524b78abc64cf-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_linux_arm:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: package_linux_arm
    - BUILD_TAGS: vault
    - BUNDLE_NAME: vault_0.0.1-sam-test
    - CGOENABLED: '0'
    - GO111MODULE: 'off'
    - GOARCH: arm
    - GOOS: linux
    - GO_VERSION: 1.12.13
    - PACKAGE_NAME: vault_0.0.1-sam-test_linux_arm
    - PACKAGE_SPEC_ID: 691ce287d89859168f03139a6452c43fe5f1c5f40d76459bbb21d2c7a9d4a35c
    - PRODUCT_EDITION: ''
    - PRODUCT_NAME: vault
    - PRODUCT_VERSION: 0.0.1-sam-test
    - PRODUCT_VERSION_MMP: 0.0.1
    - PRODUCT_VERSION_PRE: sam-test
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v2-691ce287d89859168f03139a6452c43fe5f1c5f40d76459bbb21d2c7a9d4a35c-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - run:
        command: ls -lahR dist/
        name: Dump contents of dist/
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v2-691ce287d89859168f03139a6452c43fe5f1c5f40d76459bbb21d2c7a9d4a35c-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_solaris_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: package_solaris_amd64
    - BUILD_TAGS: vault
    - BUNDLE_NAME: vault_0.0.1-sam-test
    - CGOENABLED: '0'
    - GO111MODULE: 'off'
    - GOARCH: amd64
    - GOOS: solaris
    - GO_VERSION: 1.12.13
    - PACKAGE_NAME: vault_0.0.1-sam-test_solaris_amd64
    - PACKAGE_SPEC_ID: 449cfd009e8fa82f71d751d35196b070bc8096a141b65369c9dee6229c0b851b
    - PRODUCT_EDITION: ''
    - PRODUCT_NAME: vault
    - PRODUCT_VERSION: 0.0.1-sam-test
    - PRODUCT_VERSION_MMP: 0.0.1
    - PRODUCT_VERSION_PRE: sam-test
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v2-449cfd009e8fa82f71d751d35196b070bc8096a141b65369c9dee6229c0b851b-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - run:
        command: ls -lahR dist/
        name: Dump contents of dist/
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v2-449cfd009e8fa82f71d751d35196b070bc8096a141b65369c9dee6229c0b851b-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  test-ui:
    docker:
    - image: node:10-buster
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: /src
    resource_class: medium+
    steps:
    - checkout
    - restore_cache:
        key: yarn-lock-v1-{{ checksum "ui/yarn.lock" }}
    - attach_workspace:
        at: .
    - run:
        command: |
          set -x

          # Install Chrome
          wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub \
            | apt-key add -
          echo "deb http://dl.google.com/linux/chrome/deb/ stable main" \
            | tee /etc/apt/sources.list.d/google-chrome.list
          apt-get update
          apt-get -y install google-chrome-stable
          rm /etc/apt/sources.list.d/google-chrome.list
          rm -rf /var/lib/apt/lists/* /var/cache/apt/*

          # Add ./bin to the PATH so vault binary can be run by Ember tests
          export PATH="${PWD}/bin:${PATH}"

          # Run Ember tests
          cd ui
          mkdir -p test-results/qunit
          yarn test:oss
        name: Test UI
    - store_artifacts:
        path: ui/test-results
    - store_test_results:
        path: ui/test-results
  test-ui-browserstack:
    docker:
    - image: node:10-buster
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: /src
    resource_class: medium+
    steps:
    - checkout
    - restore_cache:
        key: yarn-lock-v1-{{ checksum "ui/yarn.lock" }}
    - attach_workspace:
        at: .
    - run:
        command: |
          # Add ./bin to the PATH so vault binary can be found.
          export PATH="${PWD}"/bin:${PATH}
          make test-ui-browserstack
        name: Run Browserstack Tests
  package_freebsd_arm:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: package_freebsd_arm
    - BUILD_TAGS: vault
    - BUNDLE_NAME: vault_0.0.1-sam-test
    - CGOENABLED: '0'
    - GO111MODULE: 'off'
    - GOARCH: arm
    - GOOS: freebsd
    - GO_VERSION: 1.12.13
    - PACKAGE_NAME: vault_0.0.1-sam-test_freebsd_arm
    - PACKAGE_SPEC_ID: bf076a46c933d828c6f60682f987060f67ccce878b452704d883123284413236
    - PRODUCT_EDITION: ''
    - PRODUCT_NAME: vault
    - PRODUCT_VERSION: 0.0.1-sam-test
    - PRODUCT_VERSION_MMP: 0.0.1
    - PRODUCT_VERSION_PRE: sam-test
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v2-bf076a46c933d828c6f60682f987060f67ccce878b452704d883123284413236-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - run:
        command: ls -lahR dist/
        name: Dump contents of dist/
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v2-bf076a46c933d828c6f60682f987060f67ccce878b452704d883123284413236-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  build-go-dev:
    docker:
    - image: golang:1.12.12-buster
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: /src
    steps:
    - checkout
    - restore_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
    - attach_workspace:
        at: .
    - run:
        command: |
          # Move dev UI assets to expected location
          rm -rf ./pkg
          mkdir ./pkg

          # Build dev binary
          make bootstrap dev
        name: Build dev binary
    - persist_to_workspace:
        paths:
        - bin
        root: .
  test-go-race:
    machine: true
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: ~/src
    steps:
    - checkout
    - run:
        command: |
          sudo mkdir /go
          sudo chown -R circleci:circleci /go
        name: Allow circleci user to restore Go modules cache
    - restore_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
    - run:
        command: |
          set -x

          # Install Go
          curl -sSLO "https://dl.google.com/go/go${GO_VERSION}.linux-amd64.tar.gz"
          sudo rm -rf /usr/local/go
          sudo tar -C /usr/local -xzf "go${GO_VERSION}.linux-amd64.tar.gz"
          rm -f "go${GO_VERSION}.linux-amd64.tar.gz"
          export GOPATH=/go
          export PATH="${PATH}:${GOPATH}/bin:/usr/local/go/bin"

          # Install CircleCI CLI
          curl -sSL \
              "https://github.com/CircleCI-Public/circleci-cli/releases/download/v${CIRCLECI_CLI_VERSION}/circleci-cli_${CIRCLECI_CLI_VERSION}_linux_amd64.tar.gz" \
            | sudo tar --overwrite -xz \
              -C /usr/local/bin \
              "circleci-cli_${CIRCLECI_CLI_VERSION}_linux_amd64/circleci"

          # Split Go tests by prior test times
          package_names=$(go list \
              -tags "${GO_TAGS}" \
              ./... \
            | grep -v /integ \
            | grep -v /vendor/ \
            | sort \
            | circleci tests split --split-by=timings --timings-type=classname)

          # Install gotestsum
          curl -sSL "https://github.com/gotestyourself/gotestsum/releases/download/v${GOTESTSUM_VERSION}/gotestsum_${GOTESTSUM_VERSION}_linux_amd64.tar.gz" \
            | sudo tar --overwrite -xz -C /usr/local/bin gotestsum

          # Run tests
          make prep
          mkdir -p test-results/go-test
          CGO_ENABLED= \
            VAULT_ADDR= \
            VAULT_TOKEN= \
            VAULT_DEV_ROOT_TOKEN_ID= \
            VAULT_ACC= \
            gotestsum --format=short-verbose --junitfile test-results/go-test/results.xml -- \
              -tags "${GO_TAGS}" \
              -timeout=60m \
              -parallel=20 \
              -race \
              ${package_names}
        name: Run Go tests
        no_output_timeout: 20m
    - store_artifacts:
        path: test-results
    - store_test_results:
        path: test-results
    environment:
    - CIRCLECI_CLI_VERSION: 0.1.5546
    - GO_TAGS: null
    - GO_VERSION: 1.12.12
    - GOTESTSUM_VERSION: 0.3.3
  test-go:
    machine: true
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: ~/src
    parallelism: 2
    steps:
    - checkout
    - run:
        command: |
          sudo mkdir /go
          sudo chown -R circleci:circleci /go
        name: Allow circleci user to restore Go modules cache
    - restore_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
    - run:
        command: |
          set -x

          # Install Go
          curl -sSLO "https://dl.google.com/go/go${GO_VERSION}.linux-amd64.tar.gz"
          sudo rm -rf /usr/local/go
          sudo tar -C /usr/local -xzf "go${GO_VERSION}.linux-amd64.tar.gz"
          rm -f "go${GO_VERSION}.linux-amd64.tar.gz"
          export GOPATH=/go
          export PATH="${PATH}:${GOPATH}/bin:/usr/local/go/bin"

          # Install CircleCI CLI
          curl -sSL \
              "https://github.com/CircleCI-Public/circleci-cli/releases/download/v${CIRCLECI_CLI_VERSION}/circleci-cli_${CIRCLECI_CLI_VERSION}_linux_amd64.tar.gz" \
            | sudo tar --overwrite -xz \
              -C /usr/local/bin \
              "circleci-cli_${CIRCLECI_CLI_VERSION}_linux_amd64/circleci"

          # Split Go tests by prior test times
          package_names=$(go list \
              -tags "${GO_TAGS}" \
              ./... \
            | grep -v /integ \
            | grep -v /vendor/ \
            | sort \
            | circleci tests split --split-by=timings --timings-type=classname)

          # Install gotestsum
          curl -sSL "https://github.com/gotestyourself/gotestsum/releases/download/v${GOTESTSUM_VERSION}/gotestsum_${GOTESTSUM_VERSION}_linux_amd64.tar.gz" \
            | sudo tar --overwrite -xz -C /usr/local/bin gotestsum

          # Run tests
          make prep
          mkdir -p test-results/go-test
          CGO_ENABLED= \
            VAULT_ADDR= \
            VAULT_TOKEN= \
            VAULT_DEV_ROOT_TOKEN_ID= \
            VAULT_ACC= \
            gotestsum --format=short-verbose --junitfile test-results/go-test/results.xml -- \
              -tags "${GO_TAGS}" \
              -timeout=60m \
              -parallel=20 \
               \
              ${package_names}
        name: Run Go tests
        no_output_timeout: 20m
    - store_artifacts:
        path: test-results
    - store_test_results:
        path: test-results
    environment:
    - CIRCLECI_CLI_VERSION: 0.1.5546
    - GO_TAGS: null
    - GO_VERSION: 1.12.12
    - GOTESTSUM_VERSION: 0.3.3
  package_linux_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: package_linux_amd64
    - BUILD_TAGS: vault
    - BUNDLE_NAME: vault_0.0.1-sam-test
    - CGOENABLED: '0'
    - GO111MODULE: 'off'
    - GOARCH: amd64
    - GOOS: linux
    - GO_VERSION: 1.12.13
    - PACKAGE_NAME: vault_0.0.1-sam-test_linux_amd64
    - PACKAGE_SPEC_ID: e09fee3d496b5a60a5a256ef77e4df568ca874d68dff7e8768793c81a53b4cc0
    - PRODUCT_EDITION: ''
    - PRODUCT_NAME: vault
    - PRODUCT_VERSION: 0.0.1-sam-test
    - PRODUCT_VERSION_MMP: 0.0.1
    - PRODUCT_VERSION_PRE: sam-test
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v2-e09fee3d496b5a60a5a256ef77e4df568ca874d68dff7e8768793c81a53b4cc0-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - run:
        command: ls -lahR dist/
        name: Dump contents of dist/
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v2-e09fee3d496b5a60a5a256ef77e4df568ca874d68dff7e8768793c81a53b4cc0-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_windows_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: package_windows_amd64
    - BUILD_TAGS: vault
    - BUNDLE_NAME: vault_0.0.1-sam-test
    - CGOENABLED: '0'
    - GO111MODULE: 'off'
    - GOARCH: amd64
    - GOOS: windows
    - GO_VERSION: 1.12.13
    - PACKAGE_NAME: vault_0.0.1-sam-test_windows_amd64
    - PACKAGE_SPEC_ID: 94941cdfe3547f57b7e5d90ae490c10ad3bdaa5cee1a29db5ed20c4907237027
    - PRODUCT_EDITION: ''
    - PRODUCT_NAME: vault
    - PRODUCT_VERSION: 0.0.1-sam-test
    - PRODUCT_VERSION_MMP: 0.0.1
    - PRODUCT_VERSION_PRE: sam-test
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v2-94941cdfe3547f57b7e5d90ae490c10ad3bdaa5cee1a29db5ed20c4907237027-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - run:
        command: ls -lahR dist/
        name: Dump contents of dist/
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v2-94941cdfe3547f57b7e5d90ae490c10ad3bdaa5cee1a29db5ed20c4907237027-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_linux_386:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: package_linux_386
    - BUILD_TAGS: vault
    - BUNDLE_NAME: vault_0.0.1-sam-test
    - CGOENABLED: '0'
    - GO111MODULE: 'off'
    - GOARCH: '386'
    - GOOS: linux
    - GO_VERSION: 1.12.13
    - PACKAGE_NAME: vault_0.0.1-sam-test_linux_386
    - PACKAGE_SPEC_ID: f05fd7b1df0bdc92e518cc7e4ba2edede6d684596b7758c75805fe94ada47ecd
    - PRODUCT_EDITION: ''
    - PRODUCT_NAME: vault
    - PRODUCT_VERSION: 0.0.1-sam-test
    - PRODUCT_VERSION_MMP: 0.0.1
    - PRODUCT_VERSION_PRE: sam-test
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v2-f05fd7b1df0bdc92e518cc7e4ba2edede6d684596b7758c75805fe94ada47ecd-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - run:
        command: ls -lahR dist/
        name: Dump contents of dist/
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v2-f05fd7b1df0bdc92e518cc7e4ba2edede6d684596b7758c75805fe94ada47ecd-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_freebsd_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: package_freebsd_amd64
    - BUILD_TAGS: vault
    - BUNDLE_NAME: vault_0.0.1-sam-test
    - CGOENABLED: '0'
    - GO111MODULE: 'off'
    - GOARCH: amd64
    - GOOS: freebsd
    - GO_VERSION: 1.12.13
    - PACKAGE_NAME: vault_0.0.1-sam-test_freebsd_amd64
    - PACKAGE_SPEC_ID: 889d4d8d937831715f030c2d4255b116a10ed6c229fb30eaed32046a082d01fe
    - PRODUCT_EDITION: ''
    - PRODUCT_NAME: vault
    - PRODUCT_VERSION: 0.0.1-sam-test
    - PRODUCT_VERSION_MMP: 0.0.1
    - PRODUCT_VERSION_PRE: sam-test
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v2-889d4d8d937831715f030c2d4255b116a10ed6c229fb30eaed32046a082d01fe-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - run:
        command: ls -lahR dist/
        name: Dump contents of dist/
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v2-889d4d8d937831715f030c2d4255b116a10ed6c229fb30eaed32046a082d01fe-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_openbsd_386:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: package_openbsd_386
    - BUILD_TAGS: vault
    - BUNDLE_NAME: vault_0.0.1-sam-test
    - CGOENABLED: '0'
    - GO111MODULE: 'off'
    - GOARCH: '386'
    - GOOS: openbsd
    - GO_VERSION: 1.12.13
    - PACKAGE_NAME: vault_0.0.1-sam-test_openbsd_386
    - PACKAGE_SPEC_ID: dde1e1d385c8c7469e9d87f2fbdc4a617894064cbb98c348a4e9199f5a982a5d
    - PRODUCT_EDITION: ''
    - PRODUCT_NAME: vault
    - PRODUCT_VERSION: 0.0.1-sam-test
    - PRODUCT_VERSION_MMP: 0.0.1
    - PRODUCT_VERSION_PRE: sam-test
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v2-dde1e1d385c8c7469e9d87f2fbdc4a617894064cbb98c348a4e9199f5a982a5d-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - run:
        command: ls -lahR dist/
        name: Dump contents of dist/
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v2-dde1e1d385c8c7469e9d87f2fbdc4a617894064cbb98c348a4e9199f5a982a5d-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_openbsd_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: package_openbsd_amd64
    - BUILD_TAGS: vault
    - BUNDLE_NAME: vault_0.0.1-sam-test
    - CGOENABLED: '0'
    - GO111MODULE: 'off'
    - GOARCH: amd64
    - GOOS: openbsd
    - GO_VERSION: 1.12.13
    - PACKAGE_NAME: vault_0.0.1-sam-test_openbsd_amd64
    - PACKAGE_SPEC_ID: 7a2f2a00954e113718c4e745d1014b2c04db9b04e0dd2cc077bf07cd51e6677c
    - PRODUCT_EDITION: ''
    - PRODUCT_NAME: vault
    - PRODUCT_VERSION: 0.0.1-sam-test
    - PRODUCT_VERSION_MMP: 0.0.1
    - PRODUCT_VERSION_PRE: sam-test
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v2-7a2f2a00954e113718c4e745d1014b2c04db9b04e0dd2cc077bf07cd51e6677c-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - run:
        command: ls -lahR dist/
        name: Dump contents of dist/
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v2-7a2f2a00954e113718c4e745d1014b2c04db9b04e0dd2cc077bf07cd51e6677c-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_netbsd_386:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: package_netbsd_386
    - BUILD_TAGS: vault
    - BUNDLE_NAME: vault_0.0.1-sam-test
    - CGOENABLED: '0'
    - GO111MODULE: 'off'
    - GOARCH: '386'
    - GOOS: netbsd
    - GO_VERSION: 1.12.13
    - PACKAGE_NAME: vault_0.0.1-sam-test_netbsd_386
    - PACKAGE_SPEC_ID: 19943530bc0bcb6ed29b5ef7a4afbded0febb42fcb13a30454ab84484ca3b2d5
    - PRODUCT_EDITION: ''
    - PRODUCT_NAME: vault
    - PRODUCT_VERSION: 0.0.1-sam-test
    - PRODUCT_VERSION_MMP: 0.0.1
    - PRODUCT_VERSION_PRE: sam-test
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v2-19943530bc0bcb6ed29b5ef7a4afbded0febb42fcb13a30454ab84484ca3b2d5-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - run:
        command: ls -lahR dist/
        name: Dump contents of dist/
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v2-19943530bc0bcb6ed29b5ef7a4afbded0febb42fcb13a30454ab84484ca3b2d5-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_darwin_amd64:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: package_darwin_amd64
    - BUILD_TAGS: vault
    - BUNDLE_NAME: vault_0.0.1-sam-test
    - CGOENABLED: '0'
    - GO111MODULE: 'off'
    - GOARCH: amd64
    - GOOS: darwin
    - GO_VERSION: 1.12.13
    - PACKAGE_NAME: vault_0.0.1-sam-test_darwin_amd64
    - PACKAGE_SPEC_ID: 536b26882af2ac3de010be3091edc045a606693eecc68ca8af4f027b50d7bec3
    - PRODUCT_EDITION: ''
    - PRODUCT_NAME: vault
    - PRODUCT_VERSION: 0.0.1-sam-test
    - PRODUCT_VERSION_MMP: 0.0.1
    - PRODUCT_VERSION_PRE: sam-test
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v2-536b26882af2ac3de010be3091edc045a606693eecc68ca8af4f027b50d7bec3-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - run:
        command: ls -lahR dist/
        name: Dump contents of dist/
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v2-536b26882af2ac3de010be3091edc045a606693eecc68ca8af4f027b50d7bec3-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  package_freebsd_386:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    environment:
    - BUILD_JOB_NAME: package_freebsd_386
    - BUILD_TAGS: vault
    - BUNDLE_NAME: vault_0.0.1-sam-test
    - CGOENABLED: '0'
    - GO111MODULE: 'off'
    - GOARCH: '386'
    - GOOS: freebsd
    - GO_VERSION: 1.12.13
    - PACKAGE_NAME: vault_0.0.1-sam-test_freebsd_386
    - PACKAGE_SPEC_ID: f09b8552e9b91f13069a299a20b5ff99f312ce7bfea31efda3a63120b109c52e
    - PRODUCT_EDITION: ''
    - PRODUCT_NAME: vault
    - PRODUCT_VERSION: 0.0.1-sam-test
    - PRODUCT_VERSION_MMP: 0.0.1
    - PRODUCT_VERSION_PRE: sam-test
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        keys:
        - static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        - ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        - yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        - base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK static-load || \
          $MK ui-load || \
          $MK yarn-load || \
          $MK base-load || \
          echo "==> Unable to restore any builder images, rebuilding from scratch."
        environment:
          MK: make -f release/build/build.mk
        name: Restore static builder image (or best available).
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - restore_cache:
        key: package-cache-v2-f09b8552e9b91f13069a299a20b5ff99f312ce7bfea31efda3a63120b109c52e-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          make build
        name: Compile package
    - run:
        command: ls -lahR dist/
        name: Dump contents of dist/
    - store_artifacts:
        destination: dist/
        path: dist/
    - save_cache:
        key: package-cache-v2-f09b8552e9b91f13069a299a20b5ff99f312ce7bfea31efda3a63120b109c52e-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - dist/
  pre-flight-checks:
    docker:
    - image: circleci/buildpack-deps
    environment:
    - CCI_VERSION: 0.1.5691
    shell: /usr/bin/env bash -euo pipefail
    steps:
    - checkout
    - run:
        command: |
          export CCI_PATH=/tmp/circleci-cli/$CCI_VERSION
          mkdir -p $CCI_PATH
          NAME=circleci-cli_${CCI_VERSION}_${ARCH}
          URL=$BASE/v${CCI_VERSION}/${NAME}.tar.gz
          curl -sSL $URL \
            | tar --overwrite --strip-components=1 -xz -C $CCI_PATH "${NAME}/circleci"
          # Add circleci to the path for subsequent steps.
          echo "export PATH=$CCI_PATH:\$PATH" >> $BASH_ENV
          # Done, print some debug info.
          set -x
          . $BASH_ENV
          which circleci
          circleci version
        environment:
          ARCH: linux_amd64
          BASE: https://github.com/CircleCI-Public/circleci-cli/releases/download
        name: Install CircleCI CLI
    - run:
        command: make ci-verify
  go-mod-download:
    docker:
    - image: golang:1.12.12-buster
    shell: /usr/bin/env bash -euo pipefail -c
    working_directory: /src
    steps:
    - add_ssh_keys:
        fingerprints:
        - c6:96:98:82:dc:04:6c:39:dd:ac:83:05:e3:15:1c:98
    - checkout
    - restore_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
    - run:
        command: go mod download
        name: Download Go modules
    - run:
        command: go mod verify
        name: Verify checksums of Go modules
    - save_cache:
        key: go-sum-v1-{{ checksum "go.sum" }}
        paths:
        - /go/pkg/mod
  cache-builder-images:
    docker:
    - image: circleci/buildpack-deps
    shell: /usr/bin/env bash -euo pipefail -c
    steps:
    - setup_remote_docker
    - checkout
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - restore_cache:
        key: base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
    - restore_cache:
        key: yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
    - restore_cache:
        key: ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
    - restore_cache:
        key: static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - run:
        command: |
          $MK $NAME-load || $MK $NAME-save
        environment:
          MK: make -f release/build/build.mk
          NAME: base
        name: Update base builder image layer
    - run:
        command: |
          $MK $NAME-load || $MK $NAME-save
        environment:
          MK: make -f release/build/build.mk
          NAME: yarn
        name: Update yarn builder image layer
    - run:
        command: |
          $MK $NAME-load || $MK $NAME-save
        environment:
          MK: make -f release/build/build.mk
          NAME: ui
        name: Update ui builder image layer
    - run:
        command: |
          $MK $NAME-load || $MK $NAME-save
        environment:
          MK: make -f release/build/build.mk
          NAME: static
        name: Update static builder image layer
    - run:
        command: |
          $MK write-cache-keys
        environment:
          MK: make -f release/build/build.mk
          NAMES: static ui yarn base
        name: 'Generate builder cache keys: static ui yarn base'
    - run:
        command: |
          ls -lahR .buildcache
          docker image ls --no-trunc
        name: Info - list files in .buildcache/
    - save_cache:
        key: base-archive-v1-{{ checksum ".buildcache/base-cache-key" }}
        paths:
        - .buildcache/base
    - save_cache:
        key: yarn-archive-v1-{{ checksum ".buildcache/yarn-cache-key" }}
        paths:
        - .buildcache/yarn
    - save_cache:
        key: ui-archive-v1-{{ checksum ".buildcache/ui-cache-key" }}
        paths:
        - .buildcache/ui
    - save_cache:
        key: static-archive-v1-{{ checksum ".buildcache/static-cache-key" }}
        paths:
        - .buildcache/static
workflows:
  build-release:
    jobs:
    - cache-builder-images:
        filters:
          branches:
            only:
            - /build-.*/
            - /ci.*/
    - package_darwin_386:
        requires:
        - cache-builder-images
    - package_darwin_amd64:
        requires:
        - cache-builder-images
    - package_freebsd_386:
        requires:
        - cache-builder-images
    - package_freebsd_amd64:
        requires:
        - cache-builder-images
    - package_freebsd_arm:
        requires:
        - cache-builder-images
    - package_linux_386:
        requires:
        - cache-builder-images
    - package_linux_amd64:
        requires:
        - cache-builder-images
    - package_linux_arm:
        requires:
        - cache-builder-images
    - package_linux_arm64:
        requires:
        - cache-builder-images
    - package_netbsd_386:
        requires:
        - cache-builder-images
    - package_netbsd_amd64:
        requires:
        - cache-builder-images
    - package_openbsd_386:
        requires:
        - cache-builder-images
    - package_openbsd_amd64:
        requires:
        - cache-builder-images
    - package_solaris_amd64:
        requires:
        - cache-builder-images
    - package_windows_386:
        requires:
        - cache-builder-images
    - package_windows_amd64:
        requires:
        - cache-builder-images
    - bundle-releases:
        requires:
        - package_darwin_386
        - package_darwin_amd64
        - package_freebsd_386
        - package_freebsd_amd64
        - package_freebsd_arm
        - package_linux_386
        - package_linux_amd64
        - package_linux_arm
        - package_linux_arm64
        - package_netbsd_386
        - package_netbsd_amd64
        - package_openbsd_386
        - package_openbsd_amd64
        - package_solaris_amd64
        - package_windows_386
        - package_windows_amd64
  ci:
    jobs:
    - pre-flight-checks
    - install-ui-dependencies:
        requires:
        - pre-flight-checks
    - go-mod-download:
        requires:
        - pre-flight-checks
    - build-go-dev:
        requires:
        - go-mod-download
    - test-ui:
        requires:
        - install-ui-dependencies
        - build-go-dev
    - test-ui-browserstack:
        filters:
          branches:
            ignore: /pull\/[0-9]+/
        requires:
        - install-ui-dependencies
        - build-go-dev
    - test-go:
        requires:
        - build-go-dev
    - test-go-race:
        requires:
        - build-go-dev
  version: 2
